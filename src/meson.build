shared_module(
    'ext-workspace-manager',
    ['ext-workspace-manager.cpp'],
    dependencies: [wayfire, wayland_server, ext_workspace_protocol],
    install: true,
    install_dir: join_paths(get_option('libdir'), 'wayfire'),
)
